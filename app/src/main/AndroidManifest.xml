<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gkoeglbauer.kompasswaage" >

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Activity_Compass"
            android:screenOrientation="portrait"/>

        <activity android:name=".Activity_position_select" />

        <activity android:name=".Activity_New_Position"
            android:screenOrientation="portrait"/>

        <activity android:name=".Acitvity_Choose"
            android:screenOrientation="portrait"/>

        <activity android:name=".Activity_position_select_right" />
    </application>

</manifest>
